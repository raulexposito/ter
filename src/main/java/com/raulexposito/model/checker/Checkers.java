package com.raulexposito.model.checker;

public class Checkers {
	/*
	 * private List<Checker> checkers;
	 * 
	 * private Checkers(Checker ... checkers) { this.checkers = new
	 * ArrayList<>(Arrays.asList(checkers)); }
	 * 
	 * public static Checkers of (Checker ... checkers) { return new
	 * Checkers(checkers); }
	 */
	// public MovementResult check (Board board) {
	// checkers.forEach(checker -> checker.check(board));
	// }
}
