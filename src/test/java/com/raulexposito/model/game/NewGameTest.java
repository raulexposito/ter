package com.raulexposito.model.game;

@Deprecated
public class NewGameTest implements GameFixture {
	/*
	 * @Test public void firstTurnIsWhiteTurn() { // when Game game =
	 * createGame();
	 * 
	 * // then Assert.assertTrue(game.isTurn(WHITE)); }
	 */
}
