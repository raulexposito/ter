package com.raulexposito.model.game;

@Deprecated
public class MovementsTest implements GameFixture {
	/*
	 * private static final int A_LONG_MATCH = 1000;
	 * 
	 * @Test public void oddMovementIsBlackTurn() { Game game = createGame();
	 * doSeveralMovements(game, checkOdd, BLACK); }
	 * 
	 * @Test public void evenMovementIsWhiteTurn() { Game game = createGame();
	 * doSeveralMovements(game, checkEven, WHITE); }
	 * 
	 * private void doSeveralMovements(Game game, Checker checker, Color color)
	 * { for (int i = 0; i < A_LONG_MATCH; i++) { game.play();
	 * 
	 * if (checker.check(i)) { Assert.assertTrue(game.isTurn(color)); } } }
	 * 
	 * private interface Checker { boolean check(Integer integer); }
	 * 
	 * private Checker checkOdd = (Integer i) -> i % 2 == 0; private Checker
	 * checkEven = (Integer i) -> !checkOdd.check(i);
	 */
}
